apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: mongo
spec:
  clusterIP: None          # Headless service (IMPORTANT)
  selector:
    app: mongodb
  ports:
    - port: 27017
      targetPort: 27017
