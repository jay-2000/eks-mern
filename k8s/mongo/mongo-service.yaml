apiVersion: v1
kind: Service
metadata:
  name: mongo
  namespace: mongo
  labels:
    app: mongo
spec:
  clusterIP: None        # REQUIRED for StatefulSet DNS
  ports:
    - port: 27017
      name: mongo
  selector:
    app: mongo
