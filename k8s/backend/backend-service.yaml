# apiVersion: v1
# kind: Service
# metadata:
#   name: backend
# spec:
#   type: ClusterIP
#   selector:
#     app: backend
#   ports:
#   - name: http
#     port: 3500
#     targetPort: 3500
apiVersion: v1
kind: Service
metadata:
  name: backend
  # namespace: default
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - port: 3500
      targetPort: 3500
