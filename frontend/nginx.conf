# server {
#     listen 80;
#     server_name _;

#     root /usr/share/nginx/html;
#     index index.html;

#     # Route frontend
#     location / {
#         try_files $uri /index.html;
#     }

#     # Proxy API to backend service inside cluster
#     location /api/ {
#         proxy_pass http://backend.default.svc.cluster.local:3500/;
#     }
# }
# server {
#   listen 80;

#   location / {
#     root /usr/share/nginx/html;
#     index index.html;
#     try_files $uri /index.html;
#   }

#   location /api/ {
#     proxy_pass http://backend:3500/;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#   }
# }
server {
  listen 80;

  # Frontend (React)
  location / {
    root /usr/share/nginx/html;
    index index.html;
    try_files $uri /index.html;
  }

  # Backend API (internal only)
  location /api/ {
    proxy_pass http://backend:3500/api/;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  }
}
